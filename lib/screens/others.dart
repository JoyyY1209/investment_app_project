import 'package:flutter/material.dart';

class Others extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Others Page")),
      body: Center(child: Text("Welcome Others!")),
    );
  }
}
