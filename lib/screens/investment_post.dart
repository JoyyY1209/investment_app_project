import 'package:flutter/material.dart';

class InvestmentPost extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("InvestmentPost Page")),
      body: Center(child: Text("Welcome InvestmentPost!")),
    );
  }
}
